document.addEventListener('DOMContentLoaded', function () { const e = document.querySelectorAll('.faq__item'); e.forEach(t => { const n = t.querySelector('.faq__question'); n.addEventListener('click', () => { const c = t.classList.contains('active'); e.forEach(r => { r !== t && (r.classList.remove('active'), r.querySelector('.faq__question').setAttribute('aria-expanded', 'false')) }), t.classList.toggle('active'), n.setAttribute('aria-expanded', !c) }) }); const t = document.querySelectorAll('.animate-on-scroll'), n = { root: null, rootMargin: '0px 0px -50px 0px', threshold: .1 }, c = new IntersectionObserver(e => { e.forEach(t => { t.isIntersecting && t.target.classList.add('visible') }) }, n); t.forEach(e => { c.observe(e) }), document.querySelectorAll('a[href^="#"]').forEach(e => { e.addEventListener('click', function (t) { const n = this.getAttribute('href'); if ("#" === n) return; const c = document.querySelector(n); c && (t.preventDefault(), c.scrollIntoView({ behavior: 'smooth', block: 'start' })) }) }), document.querySelectorAll('.btn--primary').forEach(e => { e.addEventListener('click', function () { }) }) });
